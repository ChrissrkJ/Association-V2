{% extends "::base.html.twig" %}


{% block body %}


<div class="container">
  <div class=" text-info artPageTitle">
    <h3>Actus !</h3>
  </div>


  {% for a in articles %}
    <div class="row artSection">
      <div class="col-md-4 artImg">

      </div>
      <div class="col-md-8 artBody">
        <h4>{{a.title}}</h4>

        <p>
          {{ a.body}}
        </p>

        <p class="artPubliInfo">publié le | {{a.createdAt |date('d-m-Y') }}</p>
      </div>
  </div>
  {% endfor %}
</div>

{% endblock %}
